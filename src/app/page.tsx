export default function Home() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-3 px-page-x text-center">
      <span className="text-sm font-medium uppercase tracking-wide text-brand-600">
        Transportadora 3E
      </span>
      <h1 className="text-2xl font-semibold text-ink-900">
        Base do projeto pronta
      </h1>
      <p className="max-w-md text-sm text-ink-500">
        Login, painel do cliente e painel administrativo entram nas próximas
        etapas.
      </p>
    </main>
  );
}
